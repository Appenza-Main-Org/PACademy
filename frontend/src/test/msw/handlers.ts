import { type RequestHandler } from 'msw';

/**
 * MSW request handlers.
 * Handlers grow here as user-story phases add them.
 * Keep grouped by feature (auth, applicants, admin, …).
 */
export const handlers: RequestHandler[] = [];
