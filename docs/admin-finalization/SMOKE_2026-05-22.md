# Admin Production Smoke

Generated: 2026-05-22

Status: blocked locally.

The backend smoke could not be completed in this checkout because `global.json` requires `.NET SDK 10.0.300`, while this machine currently has `.NET SDK 10.0.201` as the latest installed SDK. `dotnet build backend/admin/PACademy.Admin.slnx` exits before restore/build.

Frontend static verification was run separately and is recorded in `VERIFY.md`.
