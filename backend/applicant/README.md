# PACademy Applicant Backend

Applicant backend is intentionally out of scope for the current admin-only sprint.

Boundary:

- Admin backend owns migrations and full CRUD.
- Applicant backend will be read-mostly against the shared DB.
- Do not add applicant frontend or applicant backend behavior during the admin finalization sprint.
